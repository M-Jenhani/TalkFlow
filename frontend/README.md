# Frontend (Next.js 14)

Minimal Next.js app (app router) using EventSource to consume backend SSE stream.

Run:

```powershell
cd frontend
npm install
npm run dev
```

By default the frontend calls the backend at `http://localhost:8000`. Ensure the backend is running.
